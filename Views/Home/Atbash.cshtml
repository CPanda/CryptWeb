@using System.ComponentModel.DataAnnotations
@using Microsoft.EntityFrameworkCore.Metadata
@model Atbash

@{
    ViewBag.Title = "Atbash";
    Layout = "_Layout";
}


@using (Html.BeginForm("at_encrypt", "Home", FormMethod.Post))
{
    //add more things, like if not null then show this etc. 
    @Html.Raw("</br>")
    @Html.Raw("</br>")
    @Html.TextArea("plaintext", null, new {@class = "form-control", placeholder = "Enter PlainText"})
    @Html.Raw("</br>")
    @Html.Raw("</br>")
    <input type="submit" value="Encrypt" class="btn btn-primary"/>
    if (!string.IsNullOrEmpty(Model.encrypted))
    {
        @Html.Raw(Model.encrypted + "</br>") 
    }

    Html.EndForm();
}
