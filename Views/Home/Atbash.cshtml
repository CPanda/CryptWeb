@using System.ComponentModel.DataAnnotations
@using Microsoft.EntityFrameworkCore.Metadata
@model Atbash

@{
    ViewBag.Title = "Atbash";
    Layout = "_Layout";
}


@using (Html.BeginForm("at_encrypt", "Home", FormMethod.Post))
{
    @Html.Raw("</br>")
    @Html.Raw("</br>")
    @Html.TextArea("plaintext", null, new {@class = "form-control", placeholder = "Enter PlainText or Encrypted Text"})
    @Html.Raw("</br>")
    @Html.Raw("</br>")
    <input type="submit" value="Encrypt/Decrypt" class="btn btn-primary"/>
    if (!string.IsNullOrEmpty(Model.encrypted))
    {
        @Html.Raw(Model.encrypted + "</br>") 
    }

    Html.EndForm();
}
